import styled from 'styled-components';
import { outerSpace } from '../../colors';

const S = {};

S.Title = styled.h1`
  font-weight: bold;
  font-size: 25px;
  margin-bottom: 20px;
  color: ${outerSpace};
`;

export default S;
