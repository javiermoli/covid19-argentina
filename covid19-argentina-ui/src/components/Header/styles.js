import styled from 'styled-components';
import { NavLink } from 'react-router-dom';

const S = {};

S.NavLink = styled(NavLink)`
  margin: 0 25px;
  padding: 10px;
  border-radius: 5px;
  color: #ffffff;
`;

export default S;
